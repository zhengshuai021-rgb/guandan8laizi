# 八癞子掼蛋"一键理牌"功能实现文档

> **项目位置**：`/home/admin/.openclaw/workspace/projects/guandan8laizi/`
> **整理日期**：2026-07-02 | **最后更新**：2026-07-08
> **涉及文件**：`sort_8laizi.py`、`web_server.py`、`index.html`

---

## 变更记录

### 2026-07-08（Kami）

基于 `癞子牌型多义性分析.md` 修复 3 个问题：

1. **`extract_boards` / `extract_steel_plates` 纯癞子补位缺失**
   - 原逻辑：`available` 列表要求 `c > 0`（必须有自然牌），导致 66,77+2W 无法生成 667788（木板）或 666777（钢板）
   - 修复：改为 `rank_cnt.get(r,0) + n_wilds >= N`，含纯癞子对/三张
   - 新增 Ace-high 边缘情况（Q-K-A 窗口）、同需求选高 rank tiebreaker

2. **`score()` 牌型优先级修正**
   - 原来：顺子 > 木板 > 钢板 > 三带二（与文档不符）
   - 修正为：顺子 > 钢板 > 木板 > 三带二（对齐 同花顺>炸弹>顺子>钢板>木板>三带二 的牌力层级）

3. **同花顺提取 `flush_suit_order` 排序反了**
   - 原来按花色频次降序（most_common 在前），但 `pop(0)` 取第一个元素，导致总是先取最少牌的花色，浪费癞子
   - 修复：`flush_suit_order` 按频次降序 → `pop(0)` 取最多牌的花色优先——此修复在更早提交中已合入

**验证结果**：8 种文档场景全部覆盖（顺子、三带二、木板、钢板、同花顺、炸弹均可在不同策略中生成），最优选择符合牌力优先级。

---

## 1. 功能概述

八癞子一键理牌将玩家的 27 张手牌（含 0~8 张万能癞子牌）自动编排成最优牌型组合，目标与普通掼蛋一致：**减少单张散牌**、**优先组成炸弹/同花顺/顺子/木板/钢板/三带二**。核心算法位于 `sort_8laizi.py`，枚举上千种候选方案后选择"剩余单牌最少"的结果。

### 与普通掼蛋的关键差异

| 维度 | 普通掼蛋 | 八癞子 |
|------|----------|--------|
| 癞子数量 | 2张（红桃级牌） | 0~8张（全部 4 花色 × 2 副牌 = 8 张级牌） |
| 搜索空间 | ~120 种组合 | ~2000+ 种组合 |
| 新增维度 | 无 | 癞子在炸弹 vs 同花顺之间的分配比例 |
| 同花顺 | 5张同花连续 | 严格 5 张同花连续（不可延长） |
| 策略文件 | `poker.js` k7.game.gd.sort | `sort_8laizi.py` |

---

## 2. 调用链总览

```
┌─ Web UI: 点击【一键理牌】按钮
│
├─ POST /api/sort → web_server.py api_sort()
│   └─ sort_8laizi_with_details(hand_cards)
│       ├─ 分离癞子 / 自然牌
│       ├─ 枚举所有策略组合
│       │   ├─ 方案组 A: O_flush_first      × 24排列 × (0~n_lz) bomb_wilds
│       │   ├─ 方案组 B: O_flush_single     × 24排列 × (0~n_lz) bomb_wilds
│       │   ├─ 方案组 C: N_bomb_first       × 24排列 × (0~n_lz) bomb_wilds
│       │   └─ 方案组 D: O_flush_no_straight × 18排列 × (0~n_lz) bomb_wilds
│       ├─ 按 score 排序，取最优
│       └─ 三区划分 → 返回 JSON（含 hand_hex/cards_hex 编码）
│
└─ 前端: 渲染策略对比表（前端分页，每页20条）+ 三区卡牌展示
```

---

## 3. 数据结构

### 3.1 Card（单张牌）

| 字段 | 类型 | 说明 |
|------|------|------|
| `suit` | str | 花色: S/H/C/D/X |
| `rank` | str | 点数: A/2~K/SJ/BJ |
| `is_wild` | bool | 是否为癞子（级牌） |
| `power` | int | 牌点权值（见 6.1） |
| `value` | int | 牌点数值（A=14, 2=2~K=13） |
| `cid` | int | **全局唯一标识**（`_next_cid` 计数器自动分配，不可手动指定） |

> **cid 说明**：`Card.__init__` 中若 `cid=None`，自动递增 `_next_cid` 赋值。用于 `list.remove()` 等需要区分同 rank 同花色牌的场景。

### 3.2 CardGroup（一组牌型）

| 字段 | 说明 |
|------|------|
| `cards` | list[Card] |
| `group_type` | king / flush / bomb / straight / board / steel / three_two / triple / pair / single |
| `power` | 牌型权值（见 6.2） |
| `size` | 组内张数 = len(cards) |

### 3.3 SortResult（一种理牌方案的结果）

```python
class SortResult:
    kings: list[CardGroup]         # 王炸
    flushes: list[CardGroup]       # 同花顺
    bombs: list[CardGroup]         # 炸弹
    straights: list[CardGroup]     # 顺子
    boards: list[CardGroup]        # 木板（连对）
    steels: list[CardGroup]        # 钢板（连续三张）
    three_with_twos: list[CardGroup]  # 三带二
    triples: list[CardGroup]       # 三张
    pairs: list[CardGroup]         # 对子
    singles: list[CardGroup]       # 单张
```

### 3.4 三区划分

最终显示按 `炸弹区 → 非理牌区 → 理牌右区` 顺序：

| 区 | 包含牌型 | 分区规则 |
|----|---------|---------|
| 炸弹区 (sortBombs) | 王炸、同花顺、炸弹(4~10线) | `bombs_output` getter: 王炸→同花顺→5+线炸(张数↓, sort_power↓)→4线炸(sort_power↓) |
| 非理牌区 (notsort) | 三张、对子、单张、及其他 | 不在右区的全部归此区，组间按 sort_power↓ |
| 理牌右区 (sortR) | 顺子(5张)、木板(6张)、钢板(6张)、三带二(5张) | 组 size 为 5 或 6 的特定牌型；区内排序: 三带二→顺子→木板→钢板，同类型内按 power↓ |

---

## 4. 核心算法：策略枚举

### 4.1 四大策略组

| 策略组 | 提取顺序 | 说明 |
|--------|---------|------|
| **O_flush_first** | 王炸 → 同花顺 → 炸弹 → 24排列(顺子/木板/钢板/三带二) → 三张/对子/单张 | 同花顺优先于炸弹 |
| **O_flush_single** | 同 O_flush_first，但同花顺最多取 1 个 | 避免同花顺贪心消耗过多自然牌 |
| **N_bomb_first** | 王炸 → 炸弹 → 同花顺 → 24排列 → 三张/对子/单张 | 炸弹优先于同花顺 |
| **O_flush_no_straight** | 同 O_flush_first，但去掉"顺子"提取 | 跳过顺子项（18排列） |

> **O_flush_single 说明**：`max_flushes=1`，只提取最多 1 个同花顺后把剩余牌留给炸弹。适用于手牌同花顺太多会抢走炸弹牌的情况。
>
> **注意**：`try_all_strategies`（快速入口）只用 O_flush_first + N_bomb_first + O_flush_no_straight 三组；`sort_8laizi_with_details`（Web UI 详情入口）额外包含 O_flush_single。

### 4.2 癞子分配扫描维度

八癞子相比普通掼蛋新增的关键维度：`bomb_wilds` — 炸弹可使用的癞子上限。

```python
for bomb_wilds in range(n_lz + 1):   # 0 到 全部癞子
    for order in EXTRACTION_ORDERS:   # 24 种排列
        execute_strategy(naturals, wilds, strategy, bomb_wilds, order)
```

`execute_strategy` 内部：
- **方案 O**：先调用 `extract_flush_straights(pool, wp)` 消耗癞子，再调用 `extract_bombs(pool, wp, bomb_wilds)`，炸弹最多用 `bomb_wilds` 个癞子
- **方案 N**：顺序相反，先炸弹后同花顺

总搜索量（以 3 癞子为例）：`4 × (3+1) × 24 + 3 × 18 = 396` 种策略。

### 4.3 24 种提取排列

```python
EXTRACTION_ORDERS = list(itertools.permutations(
    ["straight", "board", "steel", "three_two"]
))
# 共 4! = 24 种，如: (straight, board, steel, three_two), (board, three_two, straight, steel), ...
```

每种排列中按顺序贪心提取对应牌型。

---

## 5. 各牌型提取函数

### 5.1 王炸 (`extract_king_bombs`)

4 张大小王（2副牌共 4 张 Joker）= 1 个王炸，power = 1017。

### 5.2 同花顺 (`extract_flush_straights`)

**严格 5 张同花色连续牌**，癞子补断口。支持 A 作为高牌（10-J-Q-K-A）。

```
10 种可能的 5 卡窗口：
  A-2-3-4-5 (索引 0-4)  ~  9-10-J-Q-K (索引 8-12)  = 9 个普通窗口
  10-J-Q-K-A (索引 9-13, A 视为高牌 13) = 1 个 A 高牌窗口

对每个花色：
  遍历 10 个窗口 → 计算所需癞子数 → 选癞子最少 + 起点最高的窗口
  每 rank 最多取 1 张自然牌 → 不足 5 张用癞子补足
```

**重要约束**：同花顺不容许前后延伸超过 5 张。只从 10 个固定 5 卡窗口中选择。

**Power 公式**：`520 + 首非癞子value + max(0, 4 - 首非癞子索引)`

### 5.3 炸弹 (`extract_bombs`)

分 4 个阶段贪心提取：

| 阶段 | 规则 | 消耗癞子 |
|------|------|---------|
| Phase 0 | 纯自然炸弹（≥4 张同 rank，不消耗癞子） | 0 |
| Phase 1 | 癞子补足：3张→4线, 2张→4线, 1张→4线 | ≤ max_wilds_for_bombs |
| Phase 2 | 已有 4+ 张自然炸弹 + 癞子 = 5+ 线炸弹 | ≤ remaining |
| Phase 3 | 纯癞子炸弹（4 癞子 = 1 炸弹） | 4/个 |

**Power 公式**：`首牌power + 张数 × 100`

### 5.4 顺子 (`extract_straights`)

贪心提 5 张连续（不限花色），癞子补断口。支持 A 低牌 (A-2-3-4-5) 和 A 高牌 (10-J-Q-K-A)。
每次选癞子最少 + 长度最短的连续段。**严格限制 5 张**，不允许超长。

**Power 公式**：`首牌power + 顺子长度 - 1`

### 5.5 木板 / 连对 (`extract_boards`)

**恰好 3 个连续 rank** 各有 ≥2 张（含癞子补足）。严格限制 3 连对，不允许超长。

- 支持某 rank **0 自然牌 + 2 癞子**组成纯癞子对（例如 6677+2W → 667788，其中"8"全用癞子）
- 多个候选窗口中选癞子需求最少；同等需求选高 rank 的
- 支持 A 作为高牌（Q-K-A 木板）

### 5.6 钢板 (`extract_steel_plates`)

**恰好 2 个连续 rank** 各有 ≥3 张（含癞子补足）。严格限制 2 连三张，不允许超长。每 rank 最多取 3 张自然牌。

- 支持某 rank **0 自然牌 + 3 癞子**组成纯癞子三张（例如 66+3W → 666777，其中"7"全用癞子）
- 多个候选窗口中选癞子需求最少；同等需求选高 rank 的
- 支持 A 作为高牌（K-A 钢板）

### 5.7 三带二 (`extract_three_with_two`)

三张 + 对子，对子点数 ≤ J（`max_pair_value=11`）。贪心取可用三张 + 可用对子。

**对子选择规则**：
1. 优先选恰好 2 张的 rank（不浪费 3 张的牌）
2. 同条件下从小牌开始带（小对子优先用掉，大对子留着单独出）
3. 对子最多取 2 张（`[:min(pcnt, 2)]`）

### 5.8 残余提取 (`extract_remaining`)

三张 → 对子 → 单张，按 rank 点数降序 + 张数降序贪心。

**大小王对子处理**：在单张分组前，检查 pool 中的 SJ/BJ，2 张同类型（2 小王或 2 大王）组对子，不进入单张。

---

## 6. 排序规则

### 6.1 单牌 power（牌点权值）

| 牌 | power | 说明 |
|---|-------|------|
| 2~K | `value` | 2=2, ..., K=13 |
| A | 14 | 特殊：A 的 value 即 14 |
| 癞子（级牌） | 15 | `WILD_POWER` |
| 小王 | 16 | `SMALL_JOKER_POWER` |
| 大王 | 17 | `BIG_JOKER_POWER` |

**顺序**：普通牌 < A(14) < 癞子(15) < 小王(16) < 大王(17)

### 6.2 牌组 group.power（牌型权值）

| 牌型 | power 公式 |
|------|-----------|
| 王炸 | `1017` |
| 同花顺 | `520 + (首非癞子value + max(0, 4 - 首非癞子索引))` |
| 炸弹(4~10线) | `首牌power + 张数 × 100` |
| 顺子 | `首牌power + 顺子长度 - 1` |
| 三带二 / 三张 / 对子 / 单张 | `首牌power` |

### 6.3 sort_power（组间显示排序权值）

```python
def sort_power(g):
    for i, c in enumerate(g.cards):
        if not c.is_wild:
            return c.power + i      # 首非癞子power + 前置癞子偏移
    return g.cards[0].power         # 全癞子取首张
```

`+ i` 补偿前置癞子：癞子排在组前部，越靠前 i 越大，权值越高。

---

## 7. 方案优选规则

所有候选方案按 `score` 元组排序，越小越好：

```python
def score():
    return (
        len(singles),           # ① 单张数（优先最少）
        -len(bombs),            # ② 炸弹数（越多越好）
        -len(flushes),          # ③ 同花顺数
        -bomb5plus_count,       # ④ 5+线炸弹数
        -len(straights),        # ⑤ 顺子数
        -len(steels),           # ⑥ 钢板数（钢板 > 木板，牌力层级更高）
        -len(boards),           # ⑦ 木板数
        -len(three_with_twos),  # ⑧ 三带二数
        -len(triples),          # ⑨ 三张数
        -len(pairs),            # ⑩ 对子数
    )
```

**优先级**：单牌少 > 炸弹多 > 同花顺多 > 5+线炸多 > 顺子多 > 钢板多 > 木板多 > 三带二多 > 三张多 > 对子多

---

## 8. Web UI 架构

### 8.1 文件职责

| 文件 | 职责 |
|------|------|
| `sort_8laizi.py` | 所有理牌逻辑（Card/CardGroup/SortResult 模型、所有提取函数、策略枚举、排序、发牌、JSON 序列化、十六进制编码、测试） |
| `web_server.py` | Flask Web 服务器，路由委托给 sort_8laizi |
| `index.html` | 前端页面（深绿牌桌风格、发牌/理牌/清空按钮、三区展示、策略对比表前端分页、十六进制编码输入输出） |

### 8.2 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 返回 `index.html` |
| `/api/deal` | POST | 发牌：随机 Level=2 的 27 张手牌，返回 JSON（含 `hand_hex` 编码） |
| `/api/deal_custom` | POST | 自定义配牌：输入卡片列表，返回 JSON（含 `hand_hex` 编码） |
| `/api/sort` | POST | 理牌：输入卡片列表，返回**全量策略详情**（前端分页每页20条） + 最优结果的三区划分（含 `cards_hex` 编码） |

### 8.3 十六进制编码

**编码格式**：`0x{花色}{牌值}`，逗号分隔。

| 花色 | 编码 | 牌值 | 编码 |
|------|------|------|------|
| ♦ | 1 | A | 1 |
| ♣ | 2 | 2~9 | 2~9 |
| ♥ | 3 | 10 | A |
| ♠ | 4 | J | B |
| 王 | 5 | Q | C |
| | | K | D |
| | | 小王 | E |
| | | 大王 | F |

**API 返回字段**：
- `hand_hex`：手牌编码（/api/deal, /api/deal_custom）
- `cards_hex`：按 zone group 粒度返回编码（/api/sort）

### 8.4 公共接口（sort_8laizi 导出）

```python
sort_8laizi(hand_cards)          # → (bombs, others)  CardGroup 对象
sort_8laizi_with_details(cards)  # → dict  含 all_results + zones
deal_random_hand(level, seed)    # → [Card, ...]  27 张手牌
build_full_deck(level)           # → [(suit,rank,is_wild), ...]  108 张
cards_to_json(cards)             # → [{suit,rank,is_wild,cid}, ...]
card_to_hex(c)                   # → str  单张十六进制编码
cards_to_hex(cards)              # → str  逗号分隔的十六进制编码
```

---

## 9. 关键代码位置索引

| 功能 | sort_8laizi.py:行号 |
|------|---------------------|
| Card / CardGroup 数据结构 | 65-165 |
| 王炸提取 | 171-183 |
| 炸弹提取 | 186-260 |
| 同花顺提取 | 263-394 |
| 顺子提取 | 397-482 |
| 木板提取 | 485-537 |
| 钢板提取 | 540-599 |
| 三带二提取 | 602-668 |
| 三张/对子/单张提取（含大小王对子） | 671-733 |
| SortResult / score | 736-796 |
| 24 排列 EXTRACTION_ORDERS | 799-805 |
| execute_strategy | 808-863 |
| try_all_strategies | 866-901 |
| sort_8laizi_with_details | 924-1054 |
| 三区划分 partition_for_display | 1072-1096 |
| build_full_deck / deal_random_hand | 1099-1175 |
| cards_to_json / card_to_hex / cards_to_hex | 1177-1220 |

---

## 10. 文件结构

```
guandan8laizi/
├── sort_8laizi.py          # 核心理牌算法
├── web_server.py           # Flask Web 服务器
├── index.html              # 前端页面
├── guandan8laizi.service   # Systemd 服务配置（开机自启 + 崩溃重启）
├── start.sh                # 手动启动脚本
├── 8癞子理牌逻辑.md         # 本文档
├── 一键理牌逻辑文档.md      # 普通掼蛋理牌文档（参考）
└── README.md               # 八癞子发牌控制说明
```

---

## 11. 部署与运维

| 项目 | 说明 |
|------|------|
| **访问地址** | `http://kamika.info/guandan8laizi/` |
| **端口** | 5001（127.0.0.1） |
| **反向代理** | Caddy `handle_path /guandan8laizi/*` → `127.0.0.1:5001` |
| **服务管理** | `sudo systemctl status/restart/stop guandan8laizi` |
| **开机自启** | ✅ Restart=always, RestartSec=3 |
| **日志** | `journalctl -u guandan8laizi -f` |
| **代码更新** | `git pull` + `sudo systemctl restart guandan8laizi` |
